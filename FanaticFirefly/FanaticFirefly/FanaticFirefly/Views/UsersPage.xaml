<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converter="clr-namespace:FanaticFirefly.Helpers"
             x:Class="FanaticFirefly.Views.UsersPage">
  <ContentPage.Resources>
    <ResourceDictionary>
      <converter:StringToDateTimeConverter x:Key="stringToDateTimeConverter"></converter:StringToDateTimeConverter>
    </ResourceDictionary>
  </ContentPage.Resources>
  <ContentPage.Content>
    <ListView x:Name="listView" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>

              <Label Grid.Column="0" Grid.Row="0" Text="{Binding userName}"/>
              <Label Grid.Column="1" Grid.Row="0" Text="{Binding joinedOn, Converter={StaticResource stringToDateTimeConverter}}"/>
            </Grid>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </ContentPage.Content>
</ContentPage>