<Page
    x:Class="EuphoricElephant.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EuphoricElephant"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <ListBox Name="DevicesListBox" ItemsSource="{Binding Devices}" Height="30" RenderTransformOrigin="0.5,0.5">
                <ListBox.RenderTransform>
                    <CompositeTransform ScaleY="-1"/>
                </ListBox.RenderTransform>
            </ListBox>
            <TextBlock Text="Time" Name="TimeTextBlock"></TextBlock>
            <TextBlock Text="X:00" Name="XAccelTextBlock"></TextBlock>
            <TextBlock Text="Y:00" Name="YAccelTextBlock"></TextBlock>
            <TextBlock Text="X:00" Name="ZAccelTextBlock"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
